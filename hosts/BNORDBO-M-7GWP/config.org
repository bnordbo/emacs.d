
Load crude rego support that I made.

#+begin_src emacs-lisp
(load-file "~/.emacs.d/elisp/rego-eval.el")
#+end_src

Add capture templates.

#+begin_src emacs-lisp
(setq org-capture-templates
      (append org-capture-templates
              '(("c" "Cisco")
                ("cp" "Cisco project" entry
                 (file+headline "Projects.org" "Cisco")
                 "** TODO %i%?\nPause. Write down the problem. List three options. Pick one. Timebox. Reassess.\n")
                ("cc" "Cisco progress checklist" entry
                 (file+olp+datetree "Progress.org")
                 (file "~/.emacs.d/templates/progress-capture.org"))
                ("cd" "Cisco team lead diary" entry
                 (file+olp+datetree "Diary.org")
                 (file "~/.emacs.d/templates/work-journal-capture.org")))))
#+end_src
